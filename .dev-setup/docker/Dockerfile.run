# -----------------------------------------------------------------------------
# WARNING: This file was auto-generated by dev-setup/setup.py. 
# Do not edit directly; modify the template or input variables instead.
# -----------------------------------------------------------------------------

# Use the image from the first file
FROM robot_mindset_linux:base-1.0 AS build

# Set the working directory
WORKDIR /workspace/src

# Copy the entire bp3_integration_test folder into the src folder of the workspace
COPY . /workspace/src/robot_mindset_linux

# Set the working directory back to workspace
WORKDIR /workspace

# Make sure the install and setup scripts are executable
RUN chmod +x /workspace/src/robot_mindset_linux/.dev-setup/install.sh
RUN chmod +x /workspace/src/robot_mindset_linux/.dev-setup/setup_git_credentials.sh

# Run the installation script
RUN --mount=type=secret,id=gitcreds,dst=/tmp/.git-credentials \
    ./src/robot_mindset_linux/.dev-setup/setup_git_credentials.sh --git-credentials-file /tmp/.git-credentials
RUN ./src/robot_mindset_linux/.dev-setup/install.sh
RUN rm -rf ~/.git-credentials

# Build the workspace
RUN bash /workspace/src/robot_mindset_linux/robot_mindset_linux/install.sh

# -----------------------------------------------------------------------------
FROM robot_mindset_linux:base-1.0
COPY --from=build /root/.conan2 /root/.conan2
COPY --from=build /opt/dev-setup /opt/dev-setup
COPY --from=build /opt/robot_mindset /opt/robot_mindset
# -----------------------------------------------------------------------------

# Run the main launch file
CMD bash /opt/robot_mindset/linux/run.sh